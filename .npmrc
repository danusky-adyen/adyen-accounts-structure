# Resolve from the public registry regardless of the machine's global npm
# config. Without this, installing behind a private mirror writes that mirror's
# host into package-lock.json, and the lockfile then only installs on a machine
# that can reach it -- which fails every public CI and deploy build.
registry=https://registry.npmjs.org/
